version: 2

models:
  - name: dbt_setup_connection_test
    description: >
      Setup-check model only. This is used to test dbt installation,
      Azure SQL connection, and basic dbt tests before the real labs.
    columns:
      - name: id
        tests:
          - not_null
          - unique
